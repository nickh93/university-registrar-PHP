<!DOCTYPE html>
<html>
    {% include "partials/header.html" %}
    <body>
        <div class="container">
            <h1>Update the {{ course.getCourseName }}</h1>


            <form action="/courses/{{ course.getId }}" method="post">
              <input name="_method" type="hidden" value="patch">

              <label for="name">Rename your course:</label>
              <input id="name" name="name" type="text">

              <label for="c_number">New course number:</label>
              <input id="c_number" name="c_number" type="text">

              <button type="submit">Update</button>
            </form>

            <form action="/courses/{{ course.getId }}" method="post">
                <input name="_method" type="hidden" value="delete">

                <button type="submit">Delete this course</button>
            </form>
        </div>
    </body>
</html>
